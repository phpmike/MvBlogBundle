{% extends 'MvBlogBundle::admin-layout.html.twig' %}
{% set page_title = 'Nouvel article' %}

{% block title %}{{ page_title }} - {{ parent() }}{% endblock %}

{% block content_body %}
<h1>{{ page_title }}</h1>

<form action="{{ path('badp_post_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}

<ul class="record_actions">
    <li>
        <a href="{{ path('badp_post') }}">
            Retour à la liste
        </a>
    </li>
    <li><button type="submit">Enregistrer</button></li>
</ul>
</form>
{% endblock %}
